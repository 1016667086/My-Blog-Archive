## 一．填空题（每空2分，共30分）

1．算法的时间复杂性指算法中 **元运算** 的执行次数。

2．在忽略常数因子的情况下，O、和三个符号中， **O** 提供了算法运行时间的一个上界。

3．设Dn表示大小为n的输入集合，t(I)表示输入为I时算法的运算时间, p(I)表示输入I出现的概率，则算法的平均情况下时间复杂性A(n)= ![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/c04be3156770492fa4418fa860aa400c.png)  
 。  
 4．分治算法的时间复杂性常常满足如下形式的递归方程：  
 ![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/1b9dce6739514571ba41ca14512393c5.png)

其中，g(n)表示 **将规模为n的问题分解为子问题以及组合相应的子问题的解所需的时间** 。

5. 分治算法的基本步骤包括 **分解，递归，组合** 。

6．回溯算法的基本思想是 **在问题的状态空间树上作带剪枝的DFS搜索（或：DFS+剪枝）** 。

7．动态规划和分治法在分解子问题方面的不同点是 **前者分解出的子问题有重叠的，而后者分解出的子问题是相互独立（不重叠）的** 。

8．贪心算法中每次做出的贪心选择都是 **局部** 最优选择。

9．PQ式的分支限界法中，对于活结点表中的结点，其下界函数值越小，优先级越 **高** 。

10．选择排序、插入排序和归并排序算法中， **归并排序算法** 算法是分治算法。

11．随机算法的一个基本特征是对于同一组输入， 不同的运行可能得到 **不同** 的结果。

12.对于下面的确定性快速排序算法，只要在步骤3前加入随机化步骤 **v=random (low, high);** ，就可得到一个随机化快速排序算法，该随机化步骤的功能是 **交换A[low]和A[v]的值  
 随机选主元** 。  
 算法 QUICKSORT  
 输入：n个元素的数组A[1…n]。  
 输出：按非降序排列的数组A中的元素。

```
1.  quicksort(1, n)
end QUICKSORT
过程 quicksort(A, low, high)
// 对A[low..high]中的元素按非降序排序。
 2.  if low<high then
3.    w=SPLIT(A, low, high) 
//算法SPLIT以A[low]为主元将A[low..high]划分成两部 //分，返回主元的新位置。
4.    quicksort (A, low, w-1)
5.    quicksort (A, w+1, high)
6． end if
end quicksort

```

13．下面算法的基本运算是 **比较** 运算，该算法的时间复杂性阶为( **n** )。  
 算法 SPLIT  
 输入：正整数n，数组A[1…n]。  
 输出：…。  
 i=1  
 x=A[1]  
 for j=2 to n  
 if A[j]<=x then  
 i=i+1  
 if ij then A[i]A[j]  
 end if  
 end for  
 A[i]A[1]  
 w =i  
 return w, A  
 end SPLIT

二．计算题和简答题（每小题7分，共21分）

1．用O、、表示函数f与g之间阶的关系，并分别指出下列函数中阶最低和最高的函数：

(1)　f (n)=100 g(n)= ![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/27bcb6570fe44a218b8a6862451cf9cb.png)

(2) f(n)=6n+n

```
g(n)=3n

```

(3) f(n)= n/logn-1 g(n)=![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/c370d5f4e4894cc79071fa8502eb9461.png)

(4) f(n)=![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/df776e3deb8149b8a887af3858b8552c.png)  
 g(n)=![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/6449dbedae0a4702932a6a48929b72c7.png)

(5) f(n)= ![在这里插入图片描述](https://i-blog.csdnimg.cn/direct/d2ac14d6b9104282b87bacbc238f329b.png)  
 g(n)=